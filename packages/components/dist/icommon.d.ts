import { type ReactNode } from 'react';
import { IcommonProps } from './types';
export declare const Icommon: ({ node, width, height, size, fallbackSize, omitFill, viewBox }: IcommonProps) => ReactNode;
